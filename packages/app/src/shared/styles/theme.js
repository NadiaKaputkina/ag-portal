import { createTheme } from "@material-ui/core";

export const theme = createTheme({
    palette: {
        primary: {
            light: '#b2a429',
            main: '#000',
            dark: '#333333',
            contrastText: '#fff'
        },
        secondary: {
            light: '#aaa',
            main: '#bbb',
            dark: '#666',
        }
    }
})
