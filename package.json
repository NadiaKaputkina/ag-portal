{
  "name": "monorepo",
  "private": true,
  "main": "packages/app/src/index.js",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "npx lerna run start",
    "stop": "taskkill /f /im node.exe",
    "hoist": "npx lerna bootstrap --hoist"
  }
}
